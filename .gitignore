# dependencies
node_modules

# build output
dist
.astro

# logs
npm-debug.log

.env
.DS_Store

# screenshots from scripts/shoot.mjs
.shots

# ?inspect captures — DevTools edits waiting to be ported into the source
.inspect

# Local Netlify folder
.netlify

# The certificate studio's auto-log — every keystroke lands here, too churny
# to track; the ported design goes into cert.mjs, which is the source.
studio/cert-design.json
